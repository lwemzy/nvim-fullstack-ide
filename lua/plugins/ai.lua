-- AI integration via the Claude Code CLI (`claude -p`).
-- No ANTHROPIC_API_KEY needed — uses your existing Claude Code auth.
-- All logic is in lua/claude_cli.lua.
-- Keymaps are registered in lua/config/keymaps.lua.

return {}
